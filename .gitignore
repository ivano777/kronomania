# Godot 4+ specific ignores
.godot/
/android/

# Claude Code — machine-local config (GODOT path, permission allowlists)
.claude/settings.local.json

# Local environment — contains machine-specific paths (never commit)
.env.local

# Validation / test run log artifacts
*.log
error.txt
output.txt
validation*.txt

# repomix
repomix-output.xml

# Sprite pipeline — generated output (previews, compiled PNGs)
tools/sprites/out/

# CC0 asset-pack staging (zips + extractions; re-download via links in
# assets/sprites/combatants/LICENSE.md)
tools/sprites/packs/
